kind: compute#urlMap
name: ${URL_MAP_NAME}
defaultService: ${BES_MIG_SECURE_JUICE}
hostRules:
- hosts:
  - ${HOST_SECURE_JUICE}
  pathMatcher: secure-juice
- hosts:
  - ${HOST_INSECURE_JUICE}
  pathMatcher: insecure-juice
pathMatchers:
- defaultService: ${BES_MIG_SECURE_JUICE}
  name: secure-juice
  routeRules:
  - matchRules:
    - prefixMatch: /
    priority: 3
    service: ${BES_MIG_SECURE_JUICE}
- defaultService: ${BES_MIG_INSECURE_JUICE}
  name: insecure-juice
  routeRules:
  - matchRules:
    - prefixMatch: /
    priority: 4
    service: ${BES_MIG_INSECURE_JUICE}
